<nav class="main-header navbar navbar-expand bg-body {% if app.user is not null %}{% if app.user.skincolor == 'light' %}navbar-white navbar-light{% else %}navbar-black navbar-dark{% endif %}{% endif %}" role="navigation">
    <div class="container-fluid">
        <!-- Left navbar links -->
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
            </li>
            <li class="nav-item d-none d-sm-inline-block">
                <a href="{{ path('index') }}" class="nav-link">Home</a>
            </li>
            <li class="nav-item d-none d-sm-inline-block">
                <a href="#" class="nav-link">Contact</a>
            </li>
        </ul>

        <form class="form-inline ml-3" action="{{ path('search_entity') }}" method="post">
            <div class="input-group input-group-sm">
                <input name="searchterm" class="form-control form-control-navbar" type="search" placeholder="Search"
                       aria-label="Search">
                <div class="input-group-append">
                    <button class="btn btn-navbar" type="submit">
                        <i class="fas fa-search"></i>
                    </button>
                </div>
            </div>
        </form>
    </div>
</nav>