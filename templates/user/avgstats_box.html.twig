<div class="{{ cols }}">
    <div class="info-box{{ shadowClass }}">
        <span class="info-box-icon bg-info elevation-1">{{ icon|raw }}</span>

        <div class="info-box-content">
            <span class="info-box-text">{{ text|raw }}</span>
            <span class="info-box-number">
                  {{ value }}
            </span>
        </div>
    </div>
</div>